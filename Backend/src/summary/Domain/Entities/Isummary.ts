export interface Isummary {
  name: string
  id: `${string}-${string}-${string}-${string}-${string}`
  likes: number
  pdf: string
  sum_desc: string
  subject: string
  career: string
  lenght: number
  up_date: string
}
